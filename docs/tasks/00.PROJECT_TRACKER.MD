000: Project initialization

001: Auth Service (V1 register / login / me) — implemented on branch 001-auth-service

002: User Profile & Goals — implemented on branch 002-user-profile-goals
    Scope: users table (full schema per domain spec) + BMR/TDEE/macro
    calculation service, ported from legacy profilesetup() logic.
    Fixes: update_profile must actually persist (legacy silently didn't).
    Acceptance: PATCH /users/me recomputes and saves target_calories/

003: Food Recognition UI — implemented on branch 003-food-recognition-ui
    Scope delivered: mobile-first shell (bottom nav, no sider) and fullscreen
    `/detect`: capture → review → analyzing → results stub.
    Analyzing % is UI/request progress only (not YOLO confidence).
    Provisional DetectionService POST /api/v1/food/predict (multipart `image`,
    response typed as unknown). No YOLO, meal logging, or feedback.
    Also on this branch: inline profile edit (including name), placeholder
    avatar, empty Recommend / Tracking / Progress routes.

004: Food Prediction API / YOLO integration — implemented (detections-only JSON)
    Scope delivered: FastAPI POST /api/v1/food/predict, ONNX Runtime CPU
    (best.onnx), items/boxes/confidence, Results overlay. Analyzing % is not
    model confidence. Kcal / portion estimates are deferred (not Pipeline A
    “one box = one serving” on the wire). No torch/ultralytics in production
    deps. GET /health stays auth+users+DB; GET /api/v1/food/health is readiness.

005: Food Tracking & Daily Diary — implemented on branch 005-food-tracking-daily-diary
    Scope: Confirm & Log from Results into a meal slot; diary for Breakfast,
    Lunch, Snacks, Dinner, and Water (date prev/next on Tracking). Nutrition
    snapshots from dish_nutrition at insert (kcal + macros). Quantity = default
    servings. Seed: backend/data/dish_nutrition.csv (INDB per-100g for 27
    classes; ghevar/jalebi/bhature empty) + scripts/upsert_dish_nutrition.py.

006: Canonical food catalog — implemented on branch 006-canonical-food-catalog
    Scope: foods / aliases / servings + GET /api/v1/nutrition/search + Tracking
    Add/edit (NutritionService). Typed log: source=typed, food_id + unit.
    Scan Confirm & Log still uses dish_nutrition (005). Curated CSV only;
    the INDB workbook is not in git.

007: Scan portion V1 + Scan UX (independent of 006)
    Scope: S/M/L from dish_nutrition heuristics; Capture icons; disabled barcode;
    drop false-positive boxes. Own meal_entries grams/unit/portion_size.

008: Typed food logging — included on 006 branch (Tracking Add search + qty/unit)

009: Prediction feedback + R2 (independent of 006–008)
    Scope: thumbs, reason, optional text; R2 image vs Postgres metadata; consent.

010: Small detector expansion (independent train/eval PR)
    Scope: a few high-value classes with licensed images; not hundreds of classes.

011–014 (later): portion V2–V4, barcode decode. || Not finalized yet.

